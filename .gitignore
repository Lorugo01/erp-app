# Pastas excluídas do repositório (mantidas apenas localmente)
Servidor/
localizações/
localizacoes/

# SO / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# Secrets — único .env ativo fica na raiz; cópias locais ignoradas
.env
.env.local
erp/.env
bylab-new-api/.env
